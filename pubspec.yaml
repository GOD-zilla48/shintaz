name: shintaz_app
description: Your AI Companion App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Your existing packages
  animated_text_kit: ^4.2.2
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  
  # NEW: AI & Image Processing
  tflite_flutter: ^0.11.0        # ← FIXED: Updated from 0.10.4 to 0.11.0
  image_picker: ^1.0.4           # Camera & Gallery
  image: ^4.1.3                  # Image processing
  path_provider: ^2.1.1          # File paths

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # Assets - Updated for AI model
  assets:
    - assets/
    - assets/splash_logo.png
    - assets/shintaz_logo.png
    - assets/login_background.png
    # NEW: AI Model - Fixed filename to match your model
    - assets/models/digit_model.tflite